0.本地求生之路的目录是：E:\SteamLibrary\steamapps\common\Left 4 Dead 2

1.看来纯包分析的方案只能保证屏蔽后无法进入对应的服务器，但是游戏的组服务器列表还是会显示我们屏蔽掉的服务器。只能考虑用hook的方法来对游戏做更深入的分析来解决问题了。
2.无视会导致游戏vac的情况，我们做hook方案用来分析解决方案和想办法屏蔽那些我们讨厌的服务器。
3.通过3进行分析之后，我们的目标是写一个求生之路的vpk格式的mod来给游戏内的服务器刷新区域做按关键字屏蔽我们不想要的服务器。（屏蔽的服务器也应当同时屏蔽其ip，以免通过随机匹配功能进入那些被屏蔽的服务器。）
3.1.首选是mod给游戏内增加一个选项来配置屏蔽关键字。
3.2.其次方案是通过给mod内置一个配置文件来保存屏蔽关键字，再写一个专门配置这个mod的go+win32小程序来配置这个mod中的配置文件。（go+win32程序有一些关键的方法和配置来保证程序不会卡死，应当尤其注意这些细节，避免程序卡死）
3.3.还有一个方案是通过mod给游戏内置的服务器浏览器添加筛选功能。（这个设想由你来分析实现方案。允许通过hook分析实现可行性。）
4.此mod应当可以被steam创意工坊通过审核。（虽然我们大概率不会送去审核）。

------------

1.一些整体大网段的地址 入*.*.0.0、*.0.0.0之类的还是要识别到的，它们标记命中的话会导致大网段的内容都无法网络访问，这点还是要额外注意的。还有就是服务器的端口如果不是五位数的话也当作没有命中处理。

------------

1.服务器扫描列表还是不全，检查观察模式是否接管了游戏进程的所有流量。
2.分析所有包内服务器的所有信息。那些不活跃的服务器也要将它的包信息解出来然后放在结果列表。
3.有些服务器在游戏里显示了名称但是实际上并未真正开服，这些服务器也要着重标记。
4.有很多服务器是候选服务器，这些服务器ip不同，不要当成一条记录展示。
5.候选服务器、默认名称服务器、未获得名称的服务器、如果其延迟、人数、地图全都是未知，则也将他们作为命中条目对待。在结果列表中标记命中。且导出规则和应用屏蔽时也将他们导出和屏蔽。
6.右侧结果区域绘制成标准表格样式。不要再用伪表格了，有些结果都错位了。

---------------